requests>=2.31.0
beautifulsoup4>=4.12.0
feedparser>=6.0.10
python-dotenv>=1.0.0

# Reads the Cedefop skills-shortage Excel dataset (the EU skills source).
openpyxl>=3.1.0

# Optional LLM providers (only needed if LLM_PROVIDER is set to openai/anthropic).
# They are imported lazily, so the project runs fine without them installed.
openai>=1.0.0
anthropic>=0.18.0
